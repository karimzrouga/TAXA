{% extends 'base.html.twig' %}

{% block title %}Postes{% endblock %}

{% block body %}
    <h1>Postes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ poste.date ? poste.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ poste.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ poste.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('postes_index') }}">back to list</a>

    <a href="{{ path('postes_edit', {'id': poste.id}) }}">edit</a>

    {{ include('postes/_delete_form.html.twig') }}
{% endblock %}
