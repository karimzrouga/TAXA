{% extends 'base.html.twig' %}

{% block title %}Adventure{% endblock %}

{% block body %}
    <h1>Adventure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adventure.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ adventure.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ adventure.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ adventure.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ adventure.prix }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ adventure.date ? adventure.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adventure_index') }}">back to list</a>

    <a href="{{ path('adventure_edit', {'id': adventure.id}) }}">edit</a>

    {{ include('adventure/_delete_form.html.twig') }}
{% endblock %}
